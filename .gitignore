# Overrides ~/.gitignore_global, which hides these directories in every repository.
# Here the specs and the agent configuration are part of the project and must be versioned.
!openspec/
!.claude/

# Build artifacts
.build/
ClaudeStats.app/

.DS_Store
