# Перебивает ~/.gitignore_global, который прячет эти каталоги во всех репозиториях.
# Здесь спеки и настройки агента — часть проекта и должны версионироваться.
!openspec/
!.claude/

# Артефакты сборки
.build/
ClaudeStats.app/

.DS_Store
